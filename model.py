def analyze_media(file_path):
    # Placeholder logic — replace with real model logic
    import random
    result = "Fake" if random.random() > 0.5 else "Real"
    confidence = round(random.uniform(0.6, 0.99), 2)
    return result, confidence
